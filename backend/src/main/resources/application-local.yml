spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bidsync
    username: postgres
    password: postgres
  jpa:
    show-sql: true

bidsync:
  websocket:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
